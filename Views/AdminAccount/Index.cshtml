@model TaiKhoan

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>Thông tin tài khoản</h5>
            </div>
            <div class="ibox-content">
                <form method="get" class="form-horizontal">
                    <div class="form-group">
                        <label class="col-sm-2 control-label">Email</label>
                        <div class="col-sm-6"><span class="form-control">@Html.DisplayFor(m => Model.Email)</span></div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label">Họ</label>
                        <div class="col-sm-6"><span class="form-control">@Html.DisplayFor(m => Model.Ho)</span></div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label">Tên</label>
                        <div class="col-sm-6"><span class="form-control">@Html.DisplayFor(m => Model.Ten)</span></div>
                    </div>
                    <div class="hr-line-dashed"></div>
                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-2">
                            <button asp-action="Edit" asp-controller="AdminAccount" asp-route-id="@Model.TaiKhoanId" class="btn btn-white" type="submit">Chỉnh sửa</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>